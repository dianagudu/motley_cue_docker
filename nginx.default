server {
    listen 80;
    listen [::]:80;

    server_name ssh-oidc-web.vm.fedcloud.eu;
    server_tokens off;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://ssh-oidc-web.vm.fedcloud.eu$request_uri;
    }
}
